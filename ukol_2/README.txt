====8-puzzle solver====

The program can be run by typing the following command into the operating system's terminal
(Python 2.7 has to be installed on the system):

python run-2.py

What the program should do must be stated in the run-2.py source code (the script has no parameters).

The default behavior is to run bulk_solve_compare() method with 1000 puzzles as a parameter.
